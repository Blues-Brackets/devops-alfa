apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sample-nginx.fullname" . }}
  labels:
    {{- include "sample-nginx.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "sample-nginx.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "sample-nginx.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: nginx
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
              readOnly: true
      volumes:
        - name: html
          configMap:
            name: {{ include "sample-nginx.fullname" . }}-html
