apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-nginx.fullname" . }}
  labels:
    {{- include "sample-nginx.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "sample-nginx.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
