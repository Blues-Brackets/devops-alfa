apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - passbolt-namespace.yaml
  - passbolt-sealed-secret.yaml
  - passbolt-gpg-sealed.yaml
  - passbolt-application.yaml
  - passbolt-ingress.yaml
